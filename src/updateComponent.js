//import React, { useState, useEffect } from 'react';
/*
function UpdateComponent(props){

  const [state, setState] = useState({ nome: '', idade: ''});
    
  console.log('PROPS: ', props)
  console.log('STEPS: ', props.steps)
  console.log('STEPS.NOME: ', props.steps.nome)

  useEffect(() => {
    const nome = props.steps.nome;
    setState( nome );
  }, [props])

  
  const { nome } = state;

    return (
      <div style={{ width: '100%' }}>
        Seu nome foi atualizado, {nome}.
        
      </div>
    )
};
export default UpdateComponent; */